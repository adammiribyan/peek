// Copy to Secrets.swift and fill in your values.
// Secrets.swift is gitignored — never commit it.
enum Secrets {
    static let appToken = "your-proxy-token-here"
    static let jiraClientSecret = "your-atlassian-oauth-client-secret"
}
